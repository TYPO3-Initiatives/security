TYPO3:
  CMS:
    Policy:
      policies:
        Role:
          target: 'hasAuthority("cms:security:principal:foo") and resource.name == "cms:security:resource:foo"'
          description: 'Role policy.'
          priority: 100
          rules:
            -
              effect: permit
        Default:
          description: 'Fallback policy.'
          priority: 25
          rules:
            -
              obligation:
                deny:
                  Feedback: ['Access denied.']
